<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.bagusbayu.duelfasttap">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.DuelFastTap">
        <activity android:name=".ui.mode3.Mode3Activity"></activity>
        <activity
            android:name=".ui.mode2.Mode2Activity"
            android:label="@string/title_activity_mode2"
            android:theme="@style/Theme.DuelFastTap.NoActionBar" />
        <activity android:name=".ui.dashboard.DashboardActivity" />
        <activity android:name=".ui.splashscreen.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.mode1.Mode1Activity"
            android:screenOrientation="portrait" />
    </application>

</manifest>